require "spec_helper"

describe ToyRobotTDD do
  it "has a version number" do
    expect(ToyRobotTDD::VERSION).not_to be nil
  end

  # it "does something useful" do
  #   expect(false).to eq(true)
  # end
end
